# 指定服务端口
server:
  port: 9081

# 配置连接数据库的参数
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_ams?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

# 日志显示级别
logging:
  level:
    cn.tedu.csmall.passport: trace
